# ============================================================
# Vetra Environment Configuration (Backend & Shared)
# ============================================================

# Backend host and port
HOST=127.0.0.1
PORT=8000

# Security secret key for JWT/HMAC token signing
SECRET_KEY=vetra-production-hackathon-secret-key-2026

# Database path (SQLite)
DATABASE_PATH=server/app/database/vetra.db

# Model weights and metadata paths
MODEL_PATH=server/models/best_model.pth
METADATA_PATH=server/models/model_metadata.json
CLASS_MAPPING_PATH=server/models/class_mapping.json
BREED_INFO_PATH=server/models/breed_info.json
